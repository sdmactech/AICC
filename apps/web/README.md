# AICC Web App

Next.js version of the AI Empire Command Center. This app will replace the static prototype with data-driven views, API routes, and eventually realtime agent/event updates.

## Run

```bash
npm install
npm run dev:web
```
